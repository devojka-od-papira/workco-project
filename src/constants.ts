import { GRID_SETTINGS } from 'light-react-grid';

export const gridSettings = {
  ...GRID_SETTINGS,
  gridHelperColumnColor: 'rgba(0, 0, 0, .05)',
  gridHelperMargins: {
    sm: '5%',
    md: '5%',
    lg: '5%',
  },
};
